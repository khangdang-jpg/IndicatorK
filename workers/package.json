{
  "name": "indicatork-bot",
  "version": "1.0.0",
  "description": "Cloudflare Workers command gateway for IndicatorK Telegram bot",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}